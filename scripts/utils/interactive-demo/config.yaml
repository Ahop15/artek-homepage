# ARTEK Interactive Demo Tool Configuration

# Production URL to override
production_url: https://www.artek.tc

# Local preview server port (must match your preview server)
preview_port: 5173

# Browser Settings
browser:
  # Viewport: set to null for flexible/resizable window
  # Or specify fixed dimensions like { width: 1920, height: 1080 }
  viewport: null  # Flexible - resize window as needed